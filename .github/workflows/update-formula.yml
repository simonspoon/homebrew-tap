name: Update Formula

on:
  repository_dispatch:
    types: [update-formula]

permissions:
  contents: write

concurrency:
  group: update-formula

jobs:
  update:
    runs-on: macos-latest

    steps:
      - uses: actions/checkout@v5

      - name: Update formula
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          ./scripts/update-formula.sh \
            "${{ github.event.client_payload.tool }}" \
            "${{ github.event.client_payload.version }}" \
            "${{ github.event.client_payload.repo }}"

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add Formula/
          if git diff --staged --quiet; then
            echo "No changes to commit"
          else
            git commit -m "Update ${{ github.event.client_payload.tool }} to ${{ github.event.client_payload.version }}"
            # Releases dispatch one event per formula; near-simultaneous runs
            # can slip past the concurrency group, so rebase-and-retry the push.
            for i in 1 2 3; do
              git push && break
              git pull --rebase
            done
          fi
